# 装依赖时不跑包的生命周期脚本。
#
# 为什么必须有这一行:
#   better-sqlite3 带着 binding.gyp(源码编译用),同时在 package.json 里写了
#   "gypfile": false —— 意思是"我有预编译二进制,别自动编译我"。
#   npm 11 认这个字段;npm 10(Node 24 自带的那个版本)不认,照样去跑
#   node-gyp rebuild,于是在没装 Python + VS Build Tools 的 Windows 上直接失败:
#       gyp ERR! find Python  Could not find any Python installation to use
#   实际现场:Windows 10.0.26200 / node v24.15.0 更新 1.0.6 时卡在这里。
#
# 关掉安全吗:安全。整棵依赖树里只有 tesseract.js 有 postinstall,
# 内容是 `opencollective-postinstall || true`(捐赠横幅,本身就带 || true)。
# 原生模块 better-sqlite3 和 @napi-rs/canvas 都走预编译二进制,不需要任何脚本。
#
# 注意这条只影响装依赖,不影响 npm run —— npm start / npm run launch 照常。
ignore-scripts=true
